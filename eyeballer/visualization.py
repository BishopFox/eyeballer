from model import EyeballModel


def heatmap(model, screenshot):
    # Loop over a grid overlaid on top of the image

    partial_image = _occlude(screenshot, x, y)
    pass


def _occlude(screenshot, x, y):
    """ Occlude an image

    """

    pass
